# Resume (Private)

Private storage for personal resume.

- `Parigala_Prashanth_Kumar_Resume.pdf` — source of truth, keep up to date.
- This repo should stay **Private** on GitHub. Do not fork or share publicly.

## Update

Replace the PDF with the new version, then:

```bash
git add Parigala_Prashanth_Kumar_Resume.pdf
git commit -m "Update resume"
git push
```
